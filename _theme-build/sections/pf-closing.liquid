<!-- CLOSING CTA -->
<section class="section" style="padding:84px 0 64px;text-align:center">
  <div class="wrap">
    <span class="eyebrow">Ready when you are</span>
    <h2 style="font-size:clamp(2.2rem,4.5vw,3.4rem);font-weight:800;letter-spacing:-.035em;margin:14px 0 26px">Find your pair.</h2>
    <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap">
      <a class="btn" href="{{ routes.all_products_collection_url }}">Shop the collection</a>
      <a class="btn" href="#" style="background:transparent;color:var(--ink);border:1.5px solid var(--ink)">Bundle &amp; save up to 45%</a>
    </div>
  </div>
</section>



{% schema %}
{
  "name": "PF Closing CTA",
  "settings": [],
  "presets": [{ "name": "PF Closing CTA" }]
}
{% endschema %}
